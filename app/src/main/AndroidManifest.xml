<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.free.diary"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name="com.free.diary.DiaryApplication"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.TestCalendarActivity"
            android:label="日历"></activity>

        <activity
            android:name=".ui.activity.TestOrmActivity"
            android:label="数据库"></activity>

    </application>

</manifest>
